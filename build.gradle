plugins {
    id 'java'
    id 'application'
}
mainClassName = 'connector.Connector'

jar {
    manifest {
        attributes(
                'Class-Path': configurations.compile.collect { it.getName() }.join(' '),
                'Main-Class': 'connector.Connector'
        )
    }
}